# 项目说明

- 本项目是一个 Obsidian 学习笔记库。
- 笔记主要围绕 Python、Web 应用开发、AI 应用开发、数据处理和求职准备。
- 用户是人工智能专业大四学生，目前学习基础知识，求职方向是智能应用开发。

# 沟通要求

- 始终使用简体中文回复。
- 默认按照初学者能够理解的方式解释知识。
- 优先使用通俗语言、简单例子和可以直接运行的小段代码。
- 遇到专业术语时，先说明它是做什么的，再解释原理。
- 不要一次引入太多未学过的高级概念。

# 修改权限

- 如果用户的新要求与本文件中的现有规则、用户之前的要求或当前笔记结构存在冲突，必须先明确说明冲突点并询问用户，得到确认后再处理，不能自行选择或覆盖其中一方。
- 纯格式调整可以直接执行，不需要提前确认。
- 错别字、错误标点、标题编号等小问题可以直接修复，并在完成后说明。
- 小范围知识错误可以指出问题，但修改前应说明准备如何修改。
- 为了优化笔记结构，可以浏览整个 `学习笔记/`，并直接调整目录和笔记之间的归属关系。
- 可以直接重命名笔记文件以及笔记中的各级标题，让文件名和标题能够准确、直观地表达主题。
- 可以把一篇笔记中的部分内容移动到更相关的笔记中，也可以合并分散在多篇笔记中的同类知识点。
- 可以根据知识体系新建笔记，但不能擅自删除任何笔记。
- 认为暂时无法归类、破坏整体结构或现阶段没有必要的笔记，可以移动到 `99_待整理/` 或单独的备份分类中，不能直接删除。
- 以上移动、重命名、归并和拆分属于结构整理，可以直接执行；但不得借此大范围改写、缩减或扩充原有知识内容。
- 如果确实需要大范围补充或重写笔记内容，必须先列出修改方案并等待用户确认。
- 不要擅自删除任何非空笔记、图片或附件。

# 笔记结构整理原则

- 优先把同一类、前后关联紧密的知识点放在同一篇笔记或同一目录中，减少知识点分散和不必要的重复。
- 文件名应尽量直接使用知识点名称，避免使用 `111`、`未命名` 等难以搜索和理解的名称。
- 一级标题应与文件主题保持一致，各级标题应让用户只看标题就能大致判断内容。
- 调整结构时，以方便初学者查找、理解和按顺序学习为目标，不追求过度细分。
- 一篇笔记内容过多且包含多个独立主题时，可以合理拆分；多篇笔记内容高度重复或主题相同时，可以合理归并。
- 归并内容时必须保留原有重点和用户的学习思路，不能因为去重而遗漏知识点。
- 对调整幅度较大的原稿，优先在 `99_待整理/结构调整前备份/` 中保留归并前版本，再提供整理后的版本。
- 相关笔记之间可以添加简单的 Obsidian 内部链接，帮助用户按照学习顺序跳转，但不要添加复杂的知识图谱语法。

# Markdown 格式

- 每篇非空笔记只保留一个一级标题 `#`。
- 主要章节使用二级标题 `##`。
- 章节内的知识点使用三级标题 `###`。
- 不要跳过标题层级。
- 标题、段落、列表和代码块之间保留一个空行。
- Python 代码块使用 `python` 语言标记。
- JSON 代码块使用 `json` 语言标记。
- 终端命令使用 `bash` 语言标记。
- 流程图式文本使用 `text` 语言标记。
- 代码块必须正确闭合。
- 只有在确实有助于理解时才使用 Obsidian callout。
- 不要擅自添加 Properties、标签或复杂的 Obsidian 语法。

# 内容要求

- 保留用户原有的表达方式和学习思路。
- 笔记应以用户自己的学习视角书写；涉及学习目标、掌握程度、理解和判断时，使用“我”的第一人称表达。
- 不要在正式笔记中出现“视频里说”“老师认为”“图片中的说法”等依赖来源语境的表达，应改写为独立、明确的知识表述或“我需要掌握”的学习要求。
- 用户要求新增笔记或知识点时，必须先搜索现有笔记；如果相同内容已经完整存在，则自动忽略重复添加，并告诉用户该内容所在的文件和章节。
- 如果现有笔记只包含用户要求的一部分，则只补充缺少的内容，不重复已有内容。
- 整理格式时不得顺便改写知识内容。
- 发现知识错误时，明确区分：
  - 排版问题
  - 错别字
  - 代码运行错误
  - 知识表述错误
  - 可以补充但不是必须补充的内容
- 示例代码要尽量简单，并确保缩进和语法正确。
- 涉及可能更新的软件、框架或 API 时，优先参考官方文档。
- 不要编造用户的项目经历、性能数据或面试答案。

# 文件与附件

- 默认只整理 `学习笔记/` 中的内容。
- 除非用户明确要求，不要修改：
  - `.obsidian/`
  - `.agents/`
  - `.claude/`
  - `.copilot/`
  - `.opencode/`
  - `copilot/`
- 移动笔记时，需要同时检查图片和附件引用。
- 不要覆盖同名文件。
- `99_待整理/` 中的空白或未命名文件不能擅自删除。

# 完成后的检查

每次修改笔记后检查：

1. 标题层级是否连续。
2. 代码围栏是否闭合并标注语言。
3. 图片和附件引用是否有效。
4. 是否意外改变了无关内容。
5. 是否存在未说明的大范围修改。

# Git 操作

- 未经用户明确要求，不要执行 Git 提交、推送、回退或删除历史。
- 修改前注意已有变更，不要覆盖用户尚未保存或提交的内容。
