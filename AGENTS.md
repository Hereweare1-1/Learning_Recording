# 项目说明

- 本项目是一个 Obsidian 学习笔记库。
- 笔记主要围绕 Python、Web 应用开发、AI 应用开发、数据处理和求职准备。
- 用户是人工智能专业大四学生，目前学习基础知识，求职方向是智能应用开发。

# 沟通要求

- 始终使用简体中文回复。
- 默认按照初学者能够理解的方式解释知识。
- 优先使用通俗语言、简单例子和可以直接运行的小段代码。
- 遇到专业术语时，先说明它是做什么的，再解释原理。
- 不要一次引入太多未学过的高级概念。

# 修改权限

- 纯格式调整可以直接执行，不需要提前确认。
- 错别字、错误标点、标题编号等小问题可以直接修复，并在完成后说明。
- 小范围知识错误可以指出问题，但修改前应说明准备如何修改。
- 大范围补充、重写、合并或拆分笔记时，必须先列出修改方案并等待用户确认。
- 不要擅自删除任何非空笔记、图片或附件。
- 不要擅自重命名或移动文件，除非用户明确要求整理目录。

# Markdown 格式

- 每篇非空笔记只保留一个一级标题 `#`。
- 主要章节使用二级标题 `##`。
- 章节内的知识点使用三级标题 `###`。
- 不要跳过标题层级。
- 标题、段落、列表和代码块之间保留一个空行。
- Python 代码块使用 `python` 语言标记。
- JSON 代码块使用 `json` 语言标记。
- 终端命令使用 `bash` 语言标记。
- 流程图式文本使用 `text` 语言标记。
- 代码块必须正确闭合。
- 只有在确实有助于理解时才使用 Obsidian callout。
- 不要擅自添加 Properties、标签或复杂的 Obsidian 语法。

# 内容要求

- 保留用户原有的表达方式和学习思路。
- 整理格式时不得顺便改写知识内容。
- 发现知识错误时，明确区分：
  - 排版问题
  - 错别字
  - 代码运行错误
  - 知识表述错误
  - 可以补充但不是必须补充的内容
- 示例代码要尽量简单，并确保缩进和语法正确。
- 涉及可能更新的软件、框架或 API 时，优先参考官方文档。
- 不要编造用户的项目经历、性能数据或面试答案。

# 文件与附件

- 默认只整理 `学习笔记/` 中的内容。
- 除非用户明确要求，不要修改：
  - `.obsidian/`
  - `.agents/`
  - `.claude/`
  - `.copilot/`
  - `.opencode/`
  - `copilot/`
- 移动笔记时，需要同时检查图片和附件引用。
- 不要覆盖同名文件。
- `99_待整理/` 中的空白或未命名文件不能擅自删除。

# 完成后的检查

每次修改笔记后检查：

1. 标题层级是否连续。
2. 代码围栏是否闭合并标注语言。
3. 图片和附件引用是否有效。
4. 是否意外改变了无关内容。
5. 是否存在未说明的大范围修改。

# Git 操作

- 未经用户明确要求，不要执行 Git 提交、推送、回退或删除历史。
- 修改前注意已有变更，不要覆盖用户尚未保存或提交的内容。
