# Web网络基础

这篇笔记用于串联Web应用开发需要掌握的基础知识，具体内容分别整理在对应的主题笔记中。

## 1. 推荐学习顺序

```text
Web基础概念 → 网络通信基础 → HTTP协议 → RESTful API设计 → FastAPI学习导航
```

## 2. 主题入口

1. [[Web基础概念]]：Web网站、客户端、服务器、URL和API。
2. [[网络通信基础]]：IP地址、端口号、DNS、网络模型和TCP。
3. [[HTTP协议]]：HTTP特点、请求格式、响应格式和状态码。
4. [[RESTful API设计]]：使用URL和HTTP请求方法设计接口。
5. [[FastAPI学习导航]]：使用Python开发Web API。

## 3. 访问网站的基本过程

以访问`https://www.example.com`为例：

1. 浏览器从网址中获得域名`www.example.com`。
2. 浏览器通过DNS查询域名对应的IP地址。
3. 浏览器根据IP地址找到目标服务器。
4. 因为使用的是HTTPS，所以默认连接服务器的`443`端口。
5. 服务器中的Web程序处理请求，并把网页内容返回给浏览器。

```text
输入域名
   ↓
DNS将域名解析为IP地址
   ↓
通过IP地址找到服务器
   ↓
通过端口号找到Web程序
   ↓
Web程序返回响应
```

如果使用协议的默认端口，网址中通常不用写端口号：

```text
http://www.example.com       # 默认使用80端口
https://www.example.com      # 默认使用443端口
```

如果程序使用的不是默认端口，就需要在网址中写出来：

```text
http://127.0.0.1:8000
```
