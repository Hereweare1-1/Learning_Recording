语法：

```
try:
    # 可能出错的代码
    ...
except ValueError:
    # 值错误处理(一种异常)
    # 遇到异常执行的代码
    ...
    
except Exception as e:
    # 所有异常处理
    # 遇到异常执行的代码
    ...
    print(e)  #输出异常信息
finally:
    # 无论如何都执行（可选）
    ...
```

